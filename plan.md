1. Verify the current state of `script/audit_economy.py` and `index.html`.
2. Ensure no hardcoded old values are lingering in the logic that creates the small cards.
3. Call `submit` to push these changes to `main` branch.
